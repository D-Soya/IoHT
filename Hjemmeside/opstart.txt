# Navigate to the directory containing your virtual environment
cd /home/ioht/myenv

# Activate the virtual environment
source bin/activate


start ngrok ""ngrok http 3000"

kør flask "python app.py"
